# Perp Mini Summary (Hourly)

Data source:
- `python3 -m scripts.market_ops hourly` output `summary` and `data` fields.

Notes:
- Deterministic-first; LLM only for rendering when enabled.
- Keep WhatsApp chunks <= ~950 chars.
